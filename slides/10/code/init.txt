mkdir myawesomeproject

cd myawesomeproject

git init

#create project structure
#Note: master is the only branch available

git checkout -b develop

# add files to the staging area with git add

git commit -m "Initialize project"
